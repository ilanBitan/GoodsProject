@model IEnumerable<GoodsProject.Models.BusinessPartner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BPName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BPType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BPName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BPType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BPCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BPCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BPCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
