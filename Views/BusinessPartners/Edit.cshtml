@model GoodsProject.Models.BusinessPartner

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BusinessPartner</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BPCode" />
            <div class="form-group">
                <label asp-for="BPName" class="control-label"></label>
                <input asp-for="BPName" class="form-control" />
                <span asp-validation-for="BPName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BPType" class="control-label"></label>
                <input asp-for="BPType" class="form-control" />
                <span asp-validation-for="BPType" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Active" /> @Html.DisplayNameFor(model => model.Active)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
